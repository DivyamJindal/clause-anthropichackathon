## Packages
framer-motion | Smooth animations for page transitions and UI elements
date-fns | Date formatting for timelines and deadlines
clsx | Conditional class merging (utility)
tailwind-merge | Conflict-free tailwind class merging (utility)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Playfair Display", "serif"], // For headings, giving a legal/premium feel
  mono: ["JetBrains Mono", "monospace"], // For legal references/codes
}

API endpoints are defined in @shared/routes.
Using 'wouter' for routing.
Mock data needed if backend is empty initially.
